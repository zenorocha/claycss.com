---
title: "Home"
description: "Something nice and compelling about the project. Make it short and sweet."
---

{namespace pageIndex}

/**
 * @param site
 */
{template .render}
	{call main.render data="all"}
		{param content kind="html"}
			{call Sidebar.render data="all"}
				{param section: $site.index.children['docs'] /}
			{/call}

			<div class="sidebar-offset">
				{call .header /}
			</div>
		{/param}
	{/call}
{/template}

/**
 *
 */
{template .header}
	<header class="header home">
		<div class="container-fluid">
			<div class="row">
				<div class="col-md-10 col-md-offset-1 col-xs-10">
					<h1>A consistent design system to help you build beautiful web apps easily</h1>
				</div>
			</div>
		</div>
	</header>
{/template}